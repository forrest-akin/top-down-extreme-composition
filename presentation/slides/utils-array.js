import  { utilsArray as code } from '../code/utils-array'


const heading = 'utils/array'

const steps =
    [ ]

const utilsArray =
    { type : 'code'
    , props : { heading , code , steps } }


export  { utilsArray }
