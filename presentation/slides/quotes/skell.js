const source = 'Stephen Kell'

const [ skell1 ] =
    [ 'Current software practices fail to exploit the compositional potential within existing code; they encourage from-scratch development and coupled code. This is unfortunate, because much expense in software stems from many versions of similar things. Multiplied code means multiplied complexity and multiplied costs.'
    ].map( quote => ( { quote , source } ) )


export  { skell1 }
