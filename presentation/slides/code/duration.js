import  { domainDuration as code } from '../../code/domain-duration'
import  { createCodeSlide } from '../utils'

const heading = 'Duration Domain'
const init = { start : 1 , end : 64 , size : 22 }
const steps = []
const duration = { init , code , steps }


export  { duration }
